# App
streamlit==1.37.1

# Geo stack (binary wheels that play nice together)
numpy==1.26.4
geopandas==0.14.4
rasterio==1.3.10
shapely==2.0.4
folium==0.16.0
streamlit-folium==0.20.0

# ML
torch==2.3.1
torchvision==0.18.1
segmentation-models-pytorch==0.3.3

# Your polygon regularizer (pin to the latest that works for you; if unsure, start with this)
buildingregulariser==0.4.2

# REMOVE gdown to avoid Drive breakages
# gdown

